#!/bin/sh
# Перед публикацией: сначала гейт перед main (gate-main.sh), потом Git LFS — без него картинки и FBX не уйдут на GitHub.
# Оба читают из stdin список отправляемых ссылок, поэтому он сохраняется и отдаётся каждому.
command -v git-lfs >/dev/null 2>&1 || { printf >&2 "\n%s\n\n" "This repository is configured for Git LFS but 'git-lfs' was not found on your path. If you no longer wish to use Git LFS, remove this hook by deleting the 'pre-push' file in the hooks directory (set by 'core.hookspath'; usually '.git/hooks')."; exit 2; }
refs=$(cat)
printf '%s\n' "$refs" | "$(dirname "$0")/gate-main.sh" "$@" || exit 1
printf '%s\n' "$refs" | git lfs pre-push "$@"
