# -*- coding: utf-8 -*-
"""СИЛУЭТНЫЙ ГРАФ ЛОСЯ — поставка 5 модельной линии (`Docs/models/SPEC-priyomka-formy.md` §3).

Устройство то же, что у волка (`../wolf/graph.py`): ~20 ЖИРНЫХ узлов, радиусы — мясо и шерсть, а не кость;
формат узла — `Bone`; поле ноги кончается там, где нога на клетке 0.084 ещё держится полем, ниже — блоки
(`organs_layout.py`); морда, уши, глаза, мочка, копыта — детали, их в графе нет.

ОТКУДА ЧИСЛА. Суставы сняты с `ref/photo/moose_side_REFERENCE.jpg` по сетке 25–50 px (кропы открыты 17.09):
  земля      y = 1395 px  (низ ближнего переднего копыта; заднее опорное — 1385, кадр чуть сверху)
  холка      y =  232 px, x = 870 px (верх горба)
  масштаб    2.886 м / 1163 px — калибр игровой, пропорции натуры
Калибр 2.886 — верх места `горб` у вида (замер карты тел, земля у лося в нуле: низ капсулы 1.35 − 2.7/2). Это тот
же игровой масштаб, что у волка: холка волка 1.17 при натуре ~0.8 — ×1.46; лось в натуре ~2.0 — ×1.46 = 2.9.
Координаты: Y от земли, Z вперёд к носу (на снимке лось смотрит ВЛЕВО — Z растёт к меньшим x), X вбок (правая
сторона; левую даёт mirrorX).

ЛОСЬ НА СНИМКЕ ИДЁТ. Высоты суставов сняты С ОДНОЙ НОГИ (урок волка, `REFS.md`): передние — с ближней отвесной
(x 800–950), задние — скакательный и плюсна с отставленной назад (её плюсна отвесна, копыто оторвано от земли на
~90 px — скакательный опущен на эту высоту), колено — у складки паха. Поза сведена в «стоит»: копыта под суставами.

ШИРИНА. Анфас (`moose_front_REFERENCE.jpg`) снят в три четверти — метры по нему не читаются, только доли от ширины
головы по глазам: грудь по внешним краям передних ног ~1.7 ширины головы. Голова по глазам ≈ 0.37 длины головы
(череп лося 60 см при скуловой ширине 21 см, плюс кожа) → 0.35 м, грудь спереди ~0.60 м, бочка за локтями ~0.78 м.

Запуск:  python graph.py [--out путь]
"""
import argparse
import importlib.util
import json
import math
import os

HERE = os.path.dirname(os.path.abspath(__file__))


def _wolf_graph():
    """Перевод точек в `Bone` — один на всех, из генератора волка, а не копия (одноимённый файл — поэтому по пути)."""
    spec = importlib.util.spec_from_file_location('wolf_graph', os.path.join(HERE, '..', 'wolf', 'graph.py'))
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


_W = _wolf_graph()
to_bones, point_on = _W.to_bones, _W.point_on

# ── СНИМОК → МЕТРЫ ──────────────────────────────────────────────────────────────────────────────────
GROUND_PX, WITHERS_PX, MID_X_PX = 1395.0, 232.0, 1110.0
WITHERS_Y = 2.886
S = WITHERS_Y / (GROUND_PX - WITHERS_PX)


def px(x, y):
    """Пиксель снимка → (Z, Y) в метрах. Лось смотрит влево: Z = (MID − x)·S."""
    return ((MID_X_PX - x) * S, (GROUND_PX - y) * S)


# Замеры со снимка (пиксели исходника) — для сверки глазом по сетке
PHOTO = {
    # линия верха
    'шея_верх':        (500, 330),
    'холка_перед':     (700, 255),
    'холка_верх':      (870, 232),
    'за_холкой':       (1000, 262),
    'спина_1':         (1100, 315),
    'спина_2':         (1300, 372),
    'поясница':        (1400, 385),
    'круп_верх':       (1500, 395),
    'круп_скат':       (1600, 430),
    'круп_зад':        (1690, 520),
    'седалищный_бугор': (1705, 620),
    'зад_низ':         (1690, 780),
    # линия низа
    'горло':           (500, 640),
    'преднагрудье':    (540, 700),
    'грудь_низ':       (950, 820),
    'брюхо':           (1200, 820),
    'пах':             (1440, 800),
    # голова
    'затылок':         (430, 365),
    'основание_черепа': (445, 460),   # сустав головы: под основанием уха, за розеткой рога
    'глаз':            (318, 405),
    'лоб':             (300, 415),
    'кончик_носа':     (108, 610),
    'подбородок':      (220, 660),
    'угол_челюсти':    (350, 615),
    'серьга_низ':      (440, 795),
    # передняя (ближняя, отвесная)
    'локоть':          (840, 830),
    'запястье':        (925, 1110),
    'путо_п':          (930, 1300),
    'копыто_п':        (950, 1395),
    # задняя
    'колено':          (1520, 855),   # сустав на ~0.2 м позади складки паха (1440, 800): голень под ~50° к отвесу, а не 60°
    'скакательный':    (1720, 985),
    'путо_з':          (1700, 1300),
    'копыто_з':        (1680, 1395),
}


def P(name):
    return px(*PHOTO[name])


# Ширина ноги на снимке по высоте, м (передняя ближняя, поперёк ноги): для правила «поле не тоньше 0.1 м»
FORE_WIDTH = {1.35: 0.31, 1.23: 0.285, 1.10: 0.27, 0.98: 0.235, 0.86: 0.17, 0.73: 0.136, 0.61: 0.119, 0.48: 0.111}
# Задняя (отставленная, отвесная плюсна): под скакательным 0.21 (с пяточным сухожилием), 0.16 на Y 0.73, 0.136 на 0.48
HIND_WIDTH = {0.96: 0.21, 0.73: 0.16, 0.48: 0.136, 0.36: 0.13}

X_FORE, X_HIND = 0.19, 0.21

z_elbow, y_elbow = P('локоть')
z_wrist, y_wrist = P('запястье')
z_stifle, y_stifle = P('колено')
z_hock, y_hock = P('скакательный')
z_buttock, y_buttock = P('седалищный_бугор')
z_skull, y_skull = P('основание_черепа')
z_nose, y_nose = P('кончик_носа')

# ПЕРЕДНЯЯ. Плечевой сустав утоплен в грудь (урок волка: у преднагрудья он давал шар ВПЕРЕДИ груди)
SCAPULA_TOP = (X_FORE, 2.60, 0.80)
SHOULDER = (X_FORE, 1.92, 1.16)
ELBOW = (X_FORE, y_elbow, z_elbow)
WRIST = (X_FORE, y_wrist, z_wrist)
# ПОЛЕ ПЕРЕДНЕЙ — ДО Y 0.80. Ниже нога на снимке 0.14–0.11 м: по правилу «не тоньше 0.1» полем её можно довести до путового,
# но оболочка на 0.084 прибавляет по полклетки с боку — пясть в 0.11 м вышла бы стеблем в 0.19 с рваным краем.
# Длинная тонкая пясть — примета лося, она читается ровным бруском, а не полем
FORE_END = point_on(ELBOW, WRIST, 0.80)

# ЗАДНЯЯ. Тазобедренный внутри крупа, на трети высоты крупа вниз
HIP = (X_HIND, 2.07, -1.12)
STIFLE = (X_HIND, y_stifle, z_stifle)
HOCK = (X_HIND, y_hock, z_hock)
BUTTOCK = (X_HIND, y_buttock - 0.02, z_buttock + 0.10)
# Поле голени — до Y 1.12, над скакательным (у волка конец поля на суставе давал «шип»)
HIND_END = point_on(STIFLE, HOCK, 1.12)
THIGH_BACK_END = (X_HIND, 1.28, z_hock + 0.11)

SPINE_Y = 2.00


def node(name, socket, parent, a, b, r0, r1, section=1.0, depth=1.0, blend=0.0, mirror=False):
    return dict(name=name, socket=socket, parent=parent, a=a, b=b, r0=r0, r1=r1,
                section=section, depth=depth, blend=blend, mirror=mirror)


def build_nodes():
    # ТУША: ось бочки на середине между спиной (2.53) и брюхом (1.43); горб и шея — отдельными узлами
    spine_a = (0.0, SPINE_Y, -0.95)
    spine_b = (0.0, SPINE_Y + 0.02, 0.50)
    sacrum_a = (0.0, SPINE_Y, -0.90)
    sacrum_b = (0.0, 2.06, -1.20)
    thor_a = (0.0, SPINE_Y + 0.02, -0.10)
    thor_b = (0.0, 2.12, 0.72)
    # ЛИНИЯ ВЕРХА: горб — плато на Z 0.4…0.8 (2.87), вперёд её держит грива до затылка (2.79 на Z 1.27, 2.64 на 1.51),
    # назад — скат к спине (2.68 на Z 0.03). Итерация 1: один узел горба давал линию на 0.13–0.18 ниже снимка
    hump_a = (0.0, 2.40, -0.15)
    hump_b = (0.0, 2.60, 0.60)
    mane_b = (0.0, 2.50, 1.45)
    # ШЕЯ ГЛУБОКАЯ: низ у горла 1.88 (снимок 1.87). Итерация 1: ось на 2.30 давала низ 2.0 — горло на 0.2 выше снимка
    neck_a = (0.0, 2.24, 0.78)
    neck_b = (0.0, 2.22, z_skull - 0.02)
    # голова стоит, как на снимке: ось от основания черепа к кончику носа (−24°); узел — черепная коробка до глаз
    head_len = 0.33
    t = math.atan2(y_nose - y_skull, z_nose - z_skull)
    head_a = (0.0, y_skull, z_skull)
    head_b = (0.0, y_skull + head_len * math.sin(t), z_skull + head_len * math.cos(t))
    tail_a = (0.0, 2.26, -1.33)
    tail_b = (0.0, 2.08, -1.52)

    return [
        # ТУША — один слот `хребет`, сливается полностью
        node('хребет', 'хребет', '', spine_a, spine_b, 0.50, 0.50, section=0.74, blend=0.20),
        # КРУП: верх 2.48 на Z −0.97, зад −1.48 на высоте седалищного бугра
        # сверху круп был шире бочки (0.87 против 0.84) — у лося таз уже рёбер
        node('крестец', 'хребет', 'хребет', sacrum_a, sacrum_b, 0.46, 0.28, section=0.76, blend=0.20),
        node('грудной', 'хребет', 'хребет', thor_a, thor_b, 0.50, 0.50, section=0.72, blend=0.20),
        # ГОРБ — место `горб`: остистые отростки холки и мышца, верх 2.88 над локтем. Узкий по X — гребень, а не бочка
        node('горб', 'горб', 'грудной', hump_a, hump_b, 0.22, 0.27, section=0.62, blend=0.22),
        node('грива', 'горб', 'горб', hump_b, mane_b, 0.27, 0.18, section=0.55, blend=0.20),
        # ГРУДЬ — слот `Сердце`: киль от середины брюха до преднагрудья, низ 1.43
        # ...и до преднагрудья: перёд груди 1.41 на высоте 1.72 (итерация 1: кончался на 1.28, под шеей была ступенька)
        node('грудь', 'Сердце', 'грудной', (0.0, 1.76, -0.30), (0.0, 1.84, 1.10), 0.33, 0.40, section=0.78, blend=0.22),
        # ШЕЯ лося короткая и глубокая: от груди до основания черепа, верх 2.64 у затылка
        node('шея', 'шея', 'грудной', neck_a, neck_b, 0.44, 0.34, section=0.60, blend=0.16),
        # ГОЛОВА — только черепная коробка до глаз; морда — деталь Пасти (блок)
        # ЧЕРЕП ВЫСОКИЙ: глазницы лося у самого темени (глаз на 0.22 выше оси головы при ширине по глазам 0.44 — анфас
        # `moose_head_front.jpg`). Круглое сечение давало на высоте глаза поверхность вбок 0.047 м, и глаз висел сбоку от
        # морды (итерация 3). Направление узла менять нельзя — по нему стоят все детали головы; поэтому сечение выше
        # круга (depth 1.35) и ширина по калибру (0.44)
        node('голова', 'голова', 'шея', head_a, head_b, 0.22, 0.22, section=1.00, depth=1.35, blend=0.10),
        # СЕРЬГА — кожная складка под горлом, плоская с боков: 0.38 м вдоль тела у горла (Z 1.55…1.93), кончик на Z 1.66,
        # Y 1.49. Итерация 1: стояла на 0.2 позади снимка
        node('серьга', 'шея', 'шея', (0.0, 1.95, 1.74), (0.0, 1.55, 1.68), 0.17, 0.08,
             section=0.45, depth=1.20, blend=0.08),
        # ПЕРЕДНЯЯ
        node('лопатка', 'Руки', 'грудной', SCAPULA_TOP, SHOULDER, 0.12, 0.20, section=0.55, blend=0.22, mirror=True),
        node('плечо', 'Руки', 'лопатка', SHOULDER, ELBOW, 0.19, 0.155, section=0.80, blend=0.16, mirror=True),
        node('предплечье', 'Руки', 'плечо', ELBOW, FORE_END, 0.150, 0.085, section=0.95, blend=0.06, mirror=True),
        # ЗАДНЯЯ
        # задний край ляжки — до низа зада (1.53 на снимке) и ниже, к голени: итерация 1 оставляла дыру между бедром,
        # ляжкой и голенью на Z −1.24, Y 1.38
        node('бедро', 'Ноги', 'крестец', HIP, STIFLE, 0.34, 0.19, section=0.62, blend=0.20, mirror=True),
        node('ляжка', 'Ноги', 'бедро', BUTTOCK, THIGH_BACK_END, 0.24, 0.14, section=0.62, blend=0.18, mirror=True),
        node('голень', 'Ноги', 'бедро', STIFLE, HIND_END, 0.18, 0.10, section=0.80, blend=0.08, mirror=True),
        # ХВОСТ — обрубок под скатом крупа
        node('хвост', 'Хвост', 'крестец', tail_a, tail_b, 0.075, 0.065, blend=0.06),
    ]


HIDES = ['хребет', 'шея', 'голова', 'горб', 'Шкура', 'Сердце', 'Руки', 'Ноги', 'Хвост']


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--out', default=os.path.join(HERE, 'out', 'los-graph-draft.json'))
    args = ap.parse_args()

    nodes = build_nodes()
    bones = to_bones(nodes)
    doc = dict(species='Лось', hides=HIDES, nodes=bones)
    os.makedirs(os.path.dirname(args.out), exist_ok=True)
    with open(args.out, 'w', encoding='utf-8') as f:
        json.dump(doc, f, ensure_ascii=False, indent=2)

    print('узлов', len(bones), '→', args.out)
    for n in nodes:
        print('  %-11s %-7s  (%.2f %.2f %.2f) → (%.2f %.2f %.2f)  r %.3f→%.3f' %
              ((n['name'], n['socket']) + tuple(n['a']) + tuple(n['b']) + (n['r0'], n['r1'])))


if __name__ == '__main__':
    main()
