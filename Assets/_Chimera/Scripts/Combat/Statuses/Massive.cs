using UnityEngine;

/// <summary>
/// Маркер «массивная туша» (лось — по флагу шасси, босс — модулем боссовости): обхват игрока держит такую цель
/// НА СТАДИЮ СЛАБЕЕ (аугумент-хвост — только ст.1-замедление, защёлкнуть не выйдет). Иммунов нет —
/// есть градация по массе. Вешается генератором префаба.
/// </summary>
public class Massive : MonoBehaviour { }
