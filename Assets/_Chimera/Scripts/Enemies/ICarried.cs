/// <summary>
/// Добыча, которую хищник может НЕСТИ (3e-ii: змея тащит схваченного волка на стену-насест). Пока несут,
/// существо — кукла: своя локомоция и гравитация выключены, позицией владеет носитель (иначе стан всё равно
/// доходит до Settle и дёргает тело вниз гравитацией). На отпуск — падает с нулевой вертикальной скоростью.
/// Общий контракт по YAGNI заведён на одном носителе (волк); будущая тёплая добыча (сова/лось) ляжет сюда же.
/// </summary>
public interface ICarried
{
    void SetCarried(bool on);
}
