# Памятка оружейника (боевика) — 05.09.2026

**Доставка×Эффект:** `WindupAbility` база (замах+телеграф → Tick → Done), `Bite/Leap/Charge/Antler/Quill` + `KinVoice`, `PlayerBite/Kick/Howl` (Alt). `Hit.cs` `EffectKind` 9 (Damage…Slow) + `HitEffect` + `Hit.Apply` switch, `MeleeBlow` пайка + `Noise`.

**Статусы:** `Stagger` (стаггер<1с/стан≥1с), `Rage`×1.25/1.15, `Venom` 3 стака, `Bleed` порог 5/8, `Slow` 0.14×6, `Morale` страх↔ярость, `Knockback/Massive`, `Constrict` 3 стадии, `Satiety` Homogeneity, `Thorns/Camouflage/ColdBlooded`.

**К-Н-Б:** Зрение↔Нюх↔Термо↔Скрытность (Perception, ScentTrail, Noise, Camouflage). Эко RPS ёж→змея→волк→ёж.

**Химерный слот:** `BodySlots` 12, `chimeraSlotMult 2×`, `-1` химерного, `WornElsewhere` дубль, `Sup` схлоп. Выдача `ChimeraSlotReward` 100 родства.
