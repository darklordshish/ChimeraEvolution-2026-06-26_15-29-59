# Памятка математика (Профессор) — 05.09.2026

**Категория тела:** места-сокеты — объекты, `parent+attach` — морфизмы, дерево-позит. `имя сокета = Organ.slot` — изоморфизм механики и визуала.

**Функтор F: Spec → Game:** `SpeciesSO` → статы (`Recompute` Sup), психика (`PsycheDispatch` по `MostKin`), геометрия (`MorphBuilder`). Вес `Identity` Σ=1, химерный плавающий, дубли `Sup`. `Rebuild` — естественное преобразование.

**Клетка как ко-лимит:** `M×N` одинаковы → `radius = Σ w·table × калибр`, общее кольцо — коуравнитель, Пасть исключение, ландмарки `таз→колено…`, пояс `хребет` пушаут. Бюджет 324 квада/≈830 трис (ADR-1: хребет 8×10, было 310/800), 4 слоя спеки 14.08.

**DSL:** три закона `Тело=данные`, `Психика=код`, `Способность=доставка+эффекты` (Hit словарь). `SpeciesSO.sockets` — AST, `sizeRel` комбинатор.
