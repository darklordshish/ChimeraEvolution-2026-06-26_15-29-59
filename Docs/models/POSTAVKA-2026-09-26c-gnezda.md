# Поставка 24: гнёзда пяти видов по формату (`<вид>-places-layout.json`)

- **Кому:** линия механик (ГеймБосс) · **От:** модельная линия (Модельный) · **Дата:** 2026-09-26
- **Отвечает на:** [`FEEDBACK-2026-09-26c-format-gnezd.md`](FEEDBACK-2026-09-26c-format-gnezd.md) §3
- **Файлы:** `handoff/volk-places-layout.json`, `los-…`, `ezh-…`, `zmeya-…`, `chelovek-…`. Генератор —
  `Anatomy/tools/places_layout.py` (числа — из таблицы `GNEZDA-2026-09-26-pyat-vidov.md`, места шасси — с узлов графа).
- **Кадр:** [`kadry/гнёзда-пять-видов.png`](kadry/гнёзда-пять-видов.png) — точки и оси гнёзд поверх профиля каждого
  вида (толстый кружок — место вида, тонкий — `proposed`). Подписей нет: в шрифте стенда нет кириллицы; цвета —
  красный Пасть, голубой глаза, зелёный уши, жёлтый Рога, оранжевый Игломёт, бирюзовый Руки/Ноги, пурпурный Хвост.

---

## 1. Что в файлах

- **Все 18 мест словаря у всех пяти видов**: хребет, шея, голова, Пасть, нос, глаза, уши, ямки, Рога, Чутьё, Сердце,
  горб, Игломёт, Шкура, Руки, Ноги, Хвост, Наконечник. Скрипт падает, если у вида не хватает места или есть лишнее.
- **`proposed`**: волк 2, лось 2, ёж 4, змея 7, человек 6 — места, которых у вида нет.
- **Хозяева** — узлы графа поставки; все найдены в `<вид>-graph.json` (проверено скриптом). Змея — `звено:N` от
  головы: шея 1–4, туловище 5–9, хвост 10–15.
- **`Шкура`** — `surface {z0, z1}`; у змеи `perLink: true` (кадр на каждом звене, как договорились); у человека спина
  вертикальна — `z0/z1` там высоты Y по спине, пометил в `note`. Если формату нужна другая запись вертикальной спины,
  скажите.
- **`Рога`** — парное гнездо (`mirror`), как вы решили; у лося пара у основания его рогов.
- **`Наконечник`** у человека — на корне хвоста, по вашему §2 п. 4.
- **`горб`** — место есть только у лося (на нём грива), у остальных — `proposed` на загривке. Если `горб` не входит в
  словарь переносимых мест, выбросьте из проверки тотальности.

## 2. Исправление в таблице

В `GNEZDA-2026-09-26-pyat-vidov.md` корень хвоста змеи стоял на Z −4.15 — это ошибка, по звеньям хвост начинается
на −3.44. Исправлено там и в файле змеи; ноги змеи (✎) сдвинуты к −3.30.

## 3. Проверки

- `spec_status.py` чист; код и данные видов не трогал.
- Химеры этим файлом не меняются, пока билдер его не читает (ваш §3).
